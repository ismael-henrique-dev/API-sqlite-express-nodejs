## CRUD com node.js | express.js | sqlite

Construí essa `API` com o objetivo de melhorar meus conhecimentos em `node.js` e a sintaxe `SQL`, é um CRUD simples, porém com o necessário.

## Instalação

```bash
npm install 
```

